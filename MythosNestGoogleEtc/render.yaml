services:
  - type: web
    name: mythos-nest
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn nest_service:app --host 0.0.0.0 --port 10000
